name: sales-trainer

# Dev override: expose internal service ports on localhost for local debugging.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
  redis:
    ports:
      - "127.0.0.1:6379:6379"

  postgres:
    ports:
      - "127.0.0.1:5432:5432"
